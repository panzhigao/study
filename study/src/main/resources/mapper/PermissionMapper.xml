<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pan.mapper.PermissionMapper" >

  <resultMap id="BaseResultMap" type="com.pan.entity.Permission">
    <id     column="id"             	property="id"              jdbcType="BIGINT" />
    <result column="permission_id"      property="permissionId"    jdbcType="VARCHAR" />
    <result column="permission_name"    property="permissionName"  jdbcType="VARCHAR" />
    <result column="url"        	    property="url"             jdbcType="VARCHAR" />
    <result column="create_time"        property="createTime"      jdbcType="TIMESTAMP" />
    <result column="pid"                property="pId"             jdbcType="VARCHAR" />
    <result column="level"              property="level"           jdbcType="INTEGER" />
    <result column="sort"               property="sort"            jdbcType="INTEGER" />
    <result column="icon"               property="icon"            jdbcType="VARCHAR" />
  </resultMap>
  
  <sql id="Base_Column_List" >
    id,permission_id,permission_name,url,create_time,pid,level,sort,icon,type
  </sql>
  
  <sql id="Add_Column_List" >
    id,permission_id,permission_name,url,create_time,pid,level,sort,icon,type,create_user
  </sql>
  
  <select id="findByParams" resultMap="BaseResultMap">
  	select <include refid="Base_Column_List"/> from t_permission
  	<where>
  		<if test="permissionId !=null and permissionId !=''">
	  			and permission_id=#{permissionId}
	  	</if>
  		<if test="permissionName !=null and permissionName !=''">
	  			and permission_name like CONCAT('%','${permissionName}','%')
	  	</if>
  	</where>
  	order by create_time desc
  	<if test="offset!=null and row !=null">
  		limit #{offset},#{row}
  	</if>
  </select>
  
    <select id="getCountByParams" resultType="int">
	  	select count(0) from t_permission 
	  	<where>
	  		<if test="permissionName !=null and permissionName !=''">
	  			and permission_name like CONCAT('%','${permissionName}','%')
	  		</if>
	  	</where>
    </select>
   
  <insert id="addPermission" parameterType="com.pan.entity.Permission">
  	insert into t_permission (<include refid="Add_Column_List"/>) values 
  	(null,#{permissionId,jdbcType=VARCHAR},#{permissionName,jdbcType=VARCHAR},
  	#{url,jdbcType=VARCHAR},#{createTime,jdbcType=TIMESTAMP},#{pId,jdbcType=VARCHAR},
  	#{level,jdbcType=INTEGER},#{sort,jdbcType=INTEGER},#{icon,jdbcType=VARCHAR},#{type,jdbcType=VARCHAR},#{createUser,jdbcType=VARCHAR})
  </insert>
  
  <delete id="deletePermission">
  	delete from t_permission where permission_id=#{permissionId}
  </delete>
  
  <select id="findAll" resultMap="BaseResultMap">
 	select <include refid="Base_Column_List"/> from t_permission order by sort asc
  </select>
  
  <select id="getPermissionSelectedByRoleId" resultMap="BaseResultMap">
 	select tp.permission_id,tp.permission_name,tp.url,max(ifnull(tr.role_id,0))marker,tp.pid,tp.sort from t_permission tp		
	left join t_role_permission trp on trp.permission_id=tp.permission_id
	left join (select role_id from t_role where role_id =#{roleId}) tr on tr.role_id=trp.role_id
	group by tp.permission_id
  </select>
  
  <select id="getPermissionByRoleId" resultMap="BaseResultMap">
  	select tp.permission_id,tp.pid,tp.permission_name,tp.url,tp.icon,tp.sort,tp.type from t_role_permission trp join t_permission tp on trp.permission_id=tp.permission_id
  	where trp.role_id=#{roleId}
  </select>
  
  <update id="updatePermission">
  	update t_permission 
  	<set>
  		<if test="url !=null ">
	  			url=#{url,jdbcType=VARCHAR},
	  	</if>
  		<if test="permissionName !=null and permissionName !=''">
	  			permission_name=#{permissionName,jdbcType=VARCHAR},
	  	</if>
	  	<if test="sort !=null and sort !=''">
	  			sort=#{sort,jdbcType=INTEGER},
	  	</if>
	  	<if test="icon !=null and icon !=''">
	  			icon=#{icon,jdbcType=VARCHAR},
	  	</if>
	  	<if test="type !=null and type !=''">
	  			type=#{type,jdbcType=VARCHAR},
	  	</if>
	  	<if test="updateTime !=null">
	  			update_time=#{updateTime,jdbcType=TIMESTAMP},
	  	</if>
	  	<if test="updateUser !=null and updateUser !=''">
	  			update_user=#{updateUser,jdbcType=VARCHAR},
	  	</if>
  	</set>
  	where permission_id=#{permissionId}
  </update>
  
  <select id="findPermissionsByUserId" resultMap="BaseResultMap">
  	select tp.id,tp.permission_id,tp.permission_name,tp.url,tp.create_time,tp.pid,tp.level,tp.sort,tp.icon,tp.type,tp.create_user from t_user tu left 
  	join t_user_role tur on tu.user_id=tur.user_id 
  	join t_role_permission trp on trp.role_id=tur.role_id  
  	join t_permission tp on tp.permission_id=trp.permission_id where tu.user_id=#{userId} 
  </select>
</mapper>